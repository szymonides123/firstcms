{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-8">
    <h1 class="page-header">
                    Nazwa Bloga!
                    <small>Jakieś śmieszne motto</small>
                </h1>

    {% for post in posts %}
                    
        <h2>
            <a href="#">{{post.posttitle}}</a>
        </h2>
        <p class="lead">
            by <a href="index.php">{{post.userlogin}}</a>
        </p>
        <p><span class="glyphicon glyphicon-time"></span> {{post.postdate|date('Y-m-d H:i:s') }}</p>
        <hr>
        <img class="img-responsive" src="http://placehold.it/900x300" alt="">
        <hr>
        <div class="last-line">
            {{post.postcontent}}
        </div>
        <a class="btn btn-primary" href="#">Read More <span class="glyphicon glyphicon-chevron-right"></span></a>

        <hr>
 
    {% endfor %}
        <ul class="pager">
            {% if app.request.get('id') > 1 %}
            <li class="previous">
                <a href="{{path('blog_index',{'id': app.request.get('id')-1}) }}">&larr; Older</a>
            </li>
            {% endif %}
            {% if app.request.get('id')*3 < num[0]['num'] %}
            <li class="next">
                <a href="{{path('blog_index',{'id': app.request.get('id')+1 }) }}">Newer &rarr;</a>
            </li>
            {% endif %}
        </ul>

    {% endblock %}